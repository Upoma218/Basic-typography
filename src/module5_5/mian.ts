import methods from './utils/index';

const addition = (param1: number, param2: number): number => {
    return param1 + param2;
}

const result1 = methods.additionTwo(2, 2)
const result2 = methods.multiply(2, 2)
const result3 = methods.avarage(2, 2)
const result4 = methods.subtract(2, 2)
const result5 = methods.devide(2, 2)

